---
title: Introduction
description: What reflow is, why it exists, and what comes in the box.
---

# Introduction

`reflow` is an SSR-safe responsive toolkit for TypeScript. It combines typed breakpoints, media-query helpers, fluid typography, container-query utilities, preference detection, server-side breakpoint resolution, and framework adapters in one package.

## Why it exists

Responsive UI work usually means combining a media-query hook, viewport listener, fluid type helper, container-query utility, and a pile of SSR guards. That stack is repetitive, inconsistent, and easy to break during hydration.

`reflow` solves that by making responsive state a first-class, typed API.

## The “5 packages in 1” pitch

You can think of reflow as these tools bundled behind one API:

1. **Typed breakpoint system** — `createBreakpoints()`, `resolveResponsive()`, `useBreakpoint()`.
2. **Media and preference toolkit** — `watchMedia()`, `mq`, `useMediaQuery()`, `usePreference()`.
3. **Layout and sizing helpers** — `fluidClamp()`, `fluidScale()`, `containerQuery()`, `defineContainer()`.
4. **SSR layer** — `resolveBreakpointFromHints()`, `resolveServerBreakpoint()`, `ResponsiveProvider`.
5. **Framework adapters** — React hooks, Vue composables, Svelte stores, plus testing and Tailwind helpers.

## Key features

- **SSR-safe React hooks** powered by `useSyncExternalStore`.
- **Literal type inference** from your custom breakpoint map.
- **Modern media support** for color scheme, reduced motion, reduced data, contrast, pointer, print, retina, and foldables.
- **Native container-query helpers** for both CSS and JS.
- **Framework-agnostic core** with optional adapters for React, Vue 3, and Svelte.
- **Zero runtime dependencies** in the published library.

## Feature comparison

| Capability | react-responsive | react-use | usehooks-ts | react-device-detect | reflow |
| :-- | :--: | :--: | :--: | :--: | :--: |
| SSR-safe (zero hydration warnings) | ❌ | ⚠️ | ❌ | ❌ | ✅ |
| Typed breakpoint inference | ❌ | ❌ | ❌ | ❌ | ✅ |
| Runtime `fluidClamp()` / fluid scale | ❌ | ❌ | ❌ | ❌ | ✅ |
| Container queries | ❌ | ❌ | ❌ | ❌ | ✅ |
| `prefers-reduced-data` / `forced-colors` | ❌ | ❌ | ❌ | ❌ | ✅ |
| Client Hints / SSR breakpoint resolver | ❌ | ❌ | ❌ | ❌ | ✅ |
| Framework-agnostic core | ❌ | ❌ | ❌ | ❌ | ✅ |
| Actively maintained | ⚠️ | ⚠️ | ✅ | ❌ | ✅ |

## Architecture at a glance

```ts
import { createBreakpoints, mq, watchMedia } from "reflow";
import { fluidClamp } from "reflow/styles";
import { useBreakpoint } from "reflow/react";
import { resolveServerBreakpoint } from "reflow/server";
```

- `reflow` → framework-agnostic responsive primitives.
- `reflow/react` → React hooks and components.
- `reflow/vue` → Vue 3 composables.
- `reflow/svelte` → Svelte stores.
- `reflow/styles` → CSS string helpers.
- `reflow/server` → SSR and Client Hints helpers.
- `reflow/testing` / `reflow/tailwind` → ecosystem integrations.

## Next steps

- Go to [Installation](/getting-started/installation/).
- Build a working app with [Quick Start](/getting-started/quick-start/).
- Learn the core APIs in [Breakpoints](/core/breakpoints/) and [Media Queries](/core/media-queries/).

